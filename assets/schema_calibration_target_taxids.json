{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/dholab/baits/main/assets/schema_calibration_target_taxids.json",
    "title": "dholab/baits calibration target scope",
    "description": "Taxids treated as target-compatible when classifying calibration reads.",
    "type": "array",
    "minItems": 1,
    "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
            "taxid": {
                "oneOf": [
                    {"type": "integer", "minimum": 1},
                    {"type": "string", "pattern": "^[1-9][0-9]*$"}
                ],
                "errorMessage": "taxid must be a canonical positive NCBI Taxonomy identifier"
            }
        },
        "required": ["taxid"]
    },
    "allOf": [
        {"uniqueEntries": ["taxid"]}
    ]
}
